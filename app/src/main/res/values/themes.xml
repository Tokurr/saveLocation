<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SaveLocation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SaveLocation" parent="Base.Theme.SaveLocation" />


    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        duration. -->
        <item name="windowSplashScreenBackground">#EEF1F3</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <!-- Required for animated icons. -->
        <!-- Set the theme of the Activity that directly follows your splash
        screen. This is required. -->
        <item name="postSplashScreenTheme">@style/Theme.SaveLocation</item>

        <item name="android:windowSplashScreenBehavior" tools:targetApi="tiramisu">icon_preferred</item>

    </style>


</resources>